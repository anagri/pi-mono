/**
 * Package version constant. Bumped alongside `package.json` releases.
 *
 * Used by `BodhiPiAcpAgent.initialize()` to populate the spec-recommended
 * `agentInfo` field on `InitializeResponse`.
 */
export const BODHI_PI_VERSION = "0.0.1";
