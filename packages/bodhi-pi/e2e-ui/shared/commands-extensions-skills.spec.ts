import { test } from "../fixtures.ts";

// Requires `.bodhi-pi/commands/`, `.bodhi-pi/skills/`, `.bodhi-pi/extensions/`
// seed scenarios. Tracked as follow-up after Commit 6 once composer can hand
// project commands cleanly to the agent and skills are wired across runtimes.
test.skip("commands-extensions-skills: /say-tuesday, skill activation, extension hook", async () => {});
