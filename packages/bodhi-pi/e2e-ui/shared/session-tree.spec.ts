import { test } from "../fixtures.ts";

// Requires UI-side slash dispatch for /fork /clone /sessions /resume in
// ChatPanel composer. Tracked as follow-up after Commit 6 (matrix gate).
test.skip("session-tree: fork/clone/navigate/resume", async () => {});
