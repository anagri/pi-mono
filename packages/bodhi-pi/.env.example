# Runtime environment variables for @bodhiapp/bodhi-pi.
# The package itself does not read process.env directly — credentials and
# host-specific configuration are passed in via the AgentOptions.getApiKey
# callback. This file is reserved for future runtime config.
