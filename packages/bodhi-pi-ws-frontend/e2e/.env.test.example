# Required for ws-frontend e2e tests against real LLM providers.
# Copy this file to e2e/.env.test (gitignored) and fill in real credentials.
# Both keys are required; M11 + M12 specs fail-fast on missing ANTHROPIC_API_KEY.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
