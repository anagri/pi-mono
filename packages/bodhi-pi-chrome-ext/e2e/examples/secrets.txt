DEMO file for the redact-secrets extension. The string below LOOKS like
an Anthropic API key but is fake — feel free to commit / share this file.

API_KEY=sk-PLAINTEXTSECRETXYZ123 — should appear as [REDACTED] in the tool-call card
SLACK_TOKEN=sk-FAKE_SLACK_TOKEN_456 — same; the regex catches every `sk-…` shape
